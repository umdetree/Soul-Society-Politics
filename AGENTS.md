# AGENTS.md

## 项目性质

- 游戏开发仓库：以死神（BLEACH）世界观、护廷十三队体制为背景的长期政治模拟 RPG《瀞灵廷》。
- 目前处于企划书与系统设计撰写阶段，还没有程序实现，仓库里只有文档。
- 不要提前搭建工程、依赖、测试或原型代码；进入实现阶段由用户发起。不要去找构建、测试或 CI 命令，它们还不存在。
- 工作语言：中文。文档风格断言式、少术语黑话、无营销腔。

## 先读这两份

- `docs/GDD_v0.0.3.md`：最新高层企划（项目"宪法"；以后取序号最大的 GDD 文件）。
- `docs/systems/NPC行为模型_v0.1.1.md`：最新系统设计文档。

## 文档层级与硬性约定

- GDD（宪法）＞ 系统设计文档（部门法）＞ 数据 schema ＞ 原型。系统文档不得与 GDD 冲突，冲突以 GDD 为准。
- 规范只锁"为什么这样运转"，不锁"如何实现"；实现级内容只能放进开放问题，或明确标注的"非规范性示例"。
- 验证与规范分离：规范里不写推演故事，推演结果不得作为规范正确性的证据写进规范。
- 稳定编号发布后不得重排：GDD 用 `D-xx`（锁定决策）/ `OQ-xx`（开放问题），NPC 行为模型用 `NB-xx` / `NBQ-xx`。只更新内容与状态，不换号。
- 版本约定：GDD 每个版本新建一个文件（v0.0.1 → v0.0.2），旧版保留；系统文档版本写在文件头部。

## 文档修改

- 迭代修改时，先在当前最新版本的文件上面改，以便查看 diff；确认后再创建新版本的文档文件，并恢复之前的基础文档。

## 已废弃

- NPC 行为验证工作区（原 `tests/npc_behavior/`：世界书、场景、actor 材料、评审手册）已被用户删除，该方案不再采用；没有用户明确要求不要重建。

## Git

- 远程 `origin` → https://github.com/umdetree/Soul-Society-Politics，主分支 `main`；仓库第一个提交为 `70c80d0`。
- 提交信息用中文、简短；只在用户明确要求时提交或推送。
- 无 `.gitattributes`：提交时会报 LF→CRLF 警告，属正常，不必处理（除非用户要求）。

## 协作偏好

- 不要迎合：发现问题直接指出，包括推翻用户或既有结论。
- 用户重视方法严谨性：不要把作者写的剧情、推演或测试结果当作规范正确性的证据。
